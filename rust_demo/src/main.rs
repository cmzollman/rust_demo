    //Constant vs Mutablilty concrening Variables
    //let mut x: i32 = 5;
        //println!("The value of x is : {}", x);
        //let x : &str = "six";
        //println!("The value of x is : {}", x);

    //const SUBSCRIBER COUNT; u32 = 100,000;

    // Scalar data types
    //Integers...ie. -3,-2,-1,0,1,2,3... Non fractional. 8 bit,16 bit,32 bit, 64bit.
    //Floating-point numbers...ie. "0.12"
    //Booleans...ie. true, false
    //Characters...ie. abcd, etc...

    //Compound types...tuple
    //let tup: (&str, i32) = ("Lets tuple", 100_000);
    //let (channel: &str, sub_count: i32) = tup;
    //let sub_count: i32 = tup.1;

    //let error_codes: [i32; _] = [200,404, 500];
    //let not_found: i32; _] error_codes[1];

    //let byte: [i32; _] = [0; 8];

fn main() {
        for number in (1..4).rev() {
             println!("{}!", number);
        }
    println!("LIFTOFF!!!");
}  